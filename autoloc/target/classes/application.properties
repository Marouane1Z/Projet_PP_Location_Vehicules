spring.application.name=autoloc
# JWT
jwt.secret=autoloc_secret_key_very_long_and_secure_must_be_32_chars
jwt.expiration=86400000

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/autoloc?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=MarouaneZ1!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
# JPA ? cr�e les tables automatiquement depuis entit�s

# create-drop : Cr�e au d�marrage, supprime � l'arr�t
# create : Cr�e au d�marrage, garde � l'arr�t
# update : Met � jour le sch�ma si besoin, conserve les donn�es
# validate : V�rifie que le sch�ma correspond, ne touche � rien
# none : Ne fait rien

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Flyway
spring.flyway.enabled=false

# ?? Server ?????????????????????????????????????????????????
server.port=8081