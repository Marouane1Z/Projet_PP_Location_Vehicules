package com.autoloc.enums;

/**
 * Statut d'un véhicule dans le système.
 * Défini dans le diagramme de classes (from Vehicule).
 */

public enum statutVehicule {
    DISPONIBLE,
    LOUE,
    EN_MAINTENANCE,
    HORS_SERVICE
}